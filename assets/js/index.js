import { getBrowserWidth, getBrowserHeight } from './config.js';

const browserWidth = getBrowserWidth();
const browserHeight = getBrowserHeight();

console.log(`Width: ${browserWidth}`);
console.log(`Height: ${browserHeight}`);
